import { ADRESS } from "./ServerUrlConsts";

export const MAX_SEARCHES: number = 10;
export const LOCAL_STORAGE_USER:string = ADRESS + "userId";
export const LOCAL_STORAGE_ACCESS_TOKEN:string = ADRESS + "accessToken";